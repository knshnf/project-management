table:
  schema: public
  name: status
select_permissions:
  - role: user
    permission:
      columns:
        - color
        - created_at
        - id
        - name
        - updated_at
      filter: {}
      allow_aggregations: true
